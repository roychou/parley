- 3/3 pass on first run is suspicious until I confirm the judge catches a planted hallucination. Tomorrow's design question for technicals follows the same flavor — start with grounding, mirror the pattern.
- Chain of thought reasoning using a list of claims
- The TSLA case is the most interesting — specialist correctly identified an extreme P/E (412) and didn't get fooled into rationalizing it. Worth noting in your patterns log: the specialist's prompt is doing real work here, not just regurgitating.
- The judge correctly accepted NVDA's tilde approximations ("~65.5%") as grounded. That's the rounding tolerance in your prompt working as intended. Good.
- The judge extracted both raw metric citations ("P/E Ratio (29.9)") and qualitative characterizations ("nearly double the 20% threshold") as separate claims. That's the decomposition working. Good.
- One thing the eval is not catching, by design: whether the signal (BULLISH/BEARISH) is the right call given the data. NVDA could have come back BEARISH with all the same grounded reasoning and this eval would still pass. That's the consistency eval, which is a different problem for another day.
- Qualitative characterizations. "P/E of 12 is low" were included
- Sonnet was used throughout and burn was fine so far.
- Decompose the task. Don't ask the judge to "evaluate the reasoning." Ask it to extract every factual claim in the reasoning, then check each against the supporting data, then aggregate. Decomposed tasks produce more reliable LLM judgments than holistic ones.
- Keeping the NUMERIC_INTERPRETATION rule strict for technicals analysis: strictness is a feature when I'm calibrating, and it can loosen later if I see false negatives in real runs.
- The DIRECTIONAL/TEMPORAL rule says the data needs "sufficient temporal history (e.g., an explicit date range spanning weeks/months)". This is the right idea but the threshold is fuzzy. "Momentum is building" backed by 5 days of price action — grounded or not? The judge is going to have to guess. If your technicals specialist's supporting_technicals payload includes a defined lookback window (e.g., "20-day momentum", "50-day SMA"), the rule should reference that — i.e., the temporal claim is grounded only if the lookback window encoded in the supporting data is at least as long as the timeframe the claim implies. If supporting_technicals doesn't yet include explicit lookback metadata, that's a schema gap worth noting, not a prompt fix. Either way, capture this in notes/eval-design.md — the prompt as written will work for the planted-hallucination tests you'll write next, but real specialist outputs may surface ambiguity here.
- PATTERN's "math contradicts the pattern" — fine in principle, but the judge is an LLM, not a calculator. If a claim is "SMA-50 crossed above SMA-200" and the supporting data shows SMA-50 = 145.2 and SMA-200 = 147.8, the judge has to do the comparison itself. Sonnet will get this right most of the time but it's not foolproof on edge cases (close values, off-by-one date alignment). Not a prompt issue, more a known-limitation-of-LLM-judges issue. Worth a one-line note in notes/eval-design.md that pattern verdicts on numerically close cases should be spot-checked when you eyeball outputs. NOTE: is this where using a deterministic AI model like Aristotle might help?

