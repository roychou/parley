# macOS
.DS_Store

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
data/cache/
data/reference/
# ...except the perishable ticker->CIK map (needs FMP Premium to regenerate)
!data/reference/ticker_cik_historical.json

# Local working notes (not shared)
_plans/

# Local experiment run log (multiple-testing discipline; persists locally, not shared)
data/experiments/

# Forward paper-trading book (local, persistent track record)
data/forward/
